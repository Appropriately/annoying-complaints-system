export namespace game {
    let canvas = <HTMLCanvasElement> document.getElementById("longboi-canvas")
    let context = canvas.getContext("2d")

    export function start() {
        // TODO
    }
}
